# Server

Persistence and remote-service adapters belong here. Local SQLite storage, WeKan
REST access, attachment files, migrations, and import/export implementations stay
behind this boundary and do not render UI.
